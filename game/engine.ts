import { achievements } from '@/data/achievement-catalog';
import { activities } from '@/data/activities';
import { citySpecializations, empireUpgrades, houseTiers, items, regionTiers, scenarios, townTiers } from '@/data/content';
import { lokRuntime } from '@/integrations/lok/runtime';
import type { CustomScenarioDefinition } from './custom-scenario-types';
import { Achievement, CitySpecializationId, EmpireUpgrade, GameItem, GameState, ScenarioId } from './types';
import { EVENT_INTERVAL_MS, getActiveMarketEvent, getEventMultipliers, updateMarketEventState } from './systems/market-events';
import { calculateOfflineIncome } from './systems/offline';
import { normalizeGameState } from './systems/save';
import { portfolioEconomics } from './systems/businesses';
import { advanceCityEconomy, cityEconomySnapshot, createCityEconomyState } from './systems/city-economy';
import { incomeStreamsPerSecond, incomeStreamValue } from './systems/earnings';
import { advanceLifeHousingCosts, createLifeRpgState, gainLifeSkillXp, housingAssetValue } from './systems/life-progression';
import { advanceCareerPay, createCareerState } from './systems/careers';
import { advanceEducation, createEducationState } from './systems/education';
import { availableBuyingPower, canRiskSpend, debtInterestPerSecond, updateRiskState } from './systems/risk';
import { achievementUnlockedByRule, syncAchievementUnlocks } from './systems/achievement-rules';
import { advanceTimeSimulation, createTimeSimulationState, timeEfficiencyMultiplier } from './systems/time-simulation';
import { createGameRules } from './systems/rules';
import { applyCustomTimeSettings, customGoalLabel, normalizeCustomScenario } from './systems/custom-scenarios';
export function newGame(scenarioId:ScenarioId,mode:GameState['mode'],riskMode=false):GameState{const scenario=scenarios.find(e=>e.id===scenarioId)??scenarios[0],now=Date.now(),lok=lokRuntime.snapshot(),time=createTimeSimulationState();return{started:true,scenarioId:scenario.id,customScenario:null,mode,cash:scenario.startingCash,totalSpent:0,totalSold:0,lifetimeIncome:0,owned:{},incomeStreams:{},businesses:{},cityEconomy:createCityEconomyState(now),time,life:createLifeRpgState(false,time.gameMinute,time.settings.dayLengthMinutes),career:createCareerState(),education:createEducationState(),rules:createGameRules('standard'),houseLevel:0,townLevel:0,regionLevel:0,upgrades:{},citySpecialization:null,activeEventId:null,eventEndsAt:0,nextEventAt:now+EVENT_INTERVAL_MS,lastOfflineIncome:0,riskMode,runStatus:'active',bankruptcyDeadline:0,bankruptcyWarnings:0,peakCash:scenario.startingCash,peakNetWorth:scenario.startingCash,lowestCash:scenario.startingCash,activePlayMs:0,runAchievements:{},lokTokens:lok.balance,lokProgressMs:lok.progressMs,theme:'light',createdAt:now,updatedAt:now}}
export function newCustomGame(input:CustomScenarioDefinition):GameState{const scenario=normalizeCustomScenario(input),base=newGame('nothing',scenario.mode,scenario.riskMode);return{...base,scenarioId:'custom',customScenario:scenario,mode:scenario.mode,riskMode:scenario.riskMode,cash:scenario.startingCash,peakCash:scenario.startingCash,peakNetWorth:scenario.startingCash,lowestCash:scenario.startingCash,rules:scenario.rules,time:{...base.time,settings:applyCustomTimeSettings(base.time.settings,scenario)}}}export function normalizeState(state:GameState){return normalizeGameState(state)}
export function itemUnitPrice(item:GameItem,owned:number,state?:GameState){return item.basePrice*Math.pow(item.growthRate,owned)*(state?.rules?.economy.purchasePriceMultiplier??1)}export function itemBulkPrice(item:GameItem,owned:number,q:number,state?:GameState){if(q<=0)return 0;const m=state?.rules?.economy.purchasePriceMultiplier??1;if(item.growthRate===1)return item.basePrice*q*m;return item.basePrice*Math.pow(item.growthRate,owned)*((Math.pow(item.growthRate,q)-1)/(item.growthRate-1))*m}function upgradeIncomeMultiplier(s:GameState){return empireUpgrades.reduce((m,u)=>m*(1+(u.incomeMultiplierPerLevel??0)*(s.upgrades?.[u.id]??0)),1)}function upgradeUpkeepMultiplier(s:GameState){return empireUpgrades.reduce((m,u)=>m*Math.max(.25,1-(u.upkeepReductionPerLevel??0)*(s.upgrades?.[u.id]??0)),1)}function specializationMultipliers(s:GameState){const x=citySpecializations.find(e=>e.id===s.citySpecialization);return{income:x?.incomeMultiplier??1,upkeep:x?.upkeepMultiplier??1}}export function activeMarketEvent(s:GameState){return getActiveMarketEvent(s)}function businessEnvironment(s:GameState){const c=cityEconomySnapshot(s.cityEconomy,s.businesses??{},s.townLevel);return{demandMultiplier:c.businessDemandMultiplier*s.rules.economy.businessDemandMultiplier,laborCostMultiplier:c.laborCostMultiplier*s.rules.economy.laborCostMultiplier}}export function grossIncomePerSecond(s:GameState){const a=items.reduce((t,i)=>t+(i.incomePerSecond??0)*(s.owned[i.id]??0),0),sp=specializationMultipliers(s),e=getEventMultipliers(s),b=portfolioEconomics(s.businesses??{},businessEnvironment(s));return(a*upgradeIncomeMultiplier(s)*sp.income*e.income+b.revenuePerSecond+incomeStreamsPerSecond(s))*s.rules.economy.incomeMultiplier}export function upkeepPerSecond(s:GameState){const a=s.mode==='advanced'?items.reduce((t,i)=>t+(i.upkeepPerSecond??0)*(s.owned[i.id]??0),0):0,sp=specializationMultipliers(s),e=getEventMultipliers(s),b=portfolioEconomics(s.businesses??{},businessEnvironment(s));return(a*upgradeUpkeepMultiplier(s)*sp.upkeep*e.upkeep+b.payrollPerSecond+b.operatingCostPerSecond+debtInterestPerSecond(s)*s.rules.difficulty.debtPressureMultiplier)*s.rules.economy.costMultiplier}export function passiveCashPerSecond(s:GameState){return s.runStatus==='bankrupt'?0:(grossIncomePerSecond(s)-upkeepPerSecond(s))*timeEfficiencyMultiplier(s.time)}export function holdingsValue(s:GameState){return items.reduce((t,i)=>t+i.basePrice*(s.owned[i.id]??0),0)}export function upgradesValue(s:GameState){return empireUpgrades.reduce((t,u)=>{const l=s.upgrades?.[u.id]??0;let v=0;for(let i=0;i<l;i++)v+=u.baseCost*Math.pow(u.growthRate,i);return t+v},0)}export function netWorth(s:GameState){const h=houseTiers.find(t=>t.level===s.houseLevel)?.cost??0,tw=townTiers.find(t=>t.level===(s.townLevel??0))?.cost??0,r=regionTiers.find(t=>t.level===(s.regionLevel??0))?.cost??0;return s.cash+holdingsValue(s)+upgradesValue(s)+incomeStreamValue(s)+housingAssetValue(s.life)+h+tw+r}export function maxAffordableQuantity(s:GameState,i:GameItem){if((i.unlockSpent??0)>s.totalSpent)return 0;const o=s.owned[i.id]??0,b=s.riskMode?availableBuyingPower(s,netWorth(s)):Math.max(0,s.cash);if(b<itemUnitPrice(i,o,s))return 0;if(i.growthRate===1)return Math.floor(b/(i.basePrice*s.rules.economy.purchasePriceMultiplier));const base=i.basePrice*s.rules.economy.purchasePriceMultiplier,f=1+(b*(i.growthRate-1))/(base*Math.pow(i.growthRate,o));return Math.max(0,Math.floor(Math.log(f)/Math.log(i.growthRate)))}export function canBuyItem(s:GameState,i:GameItem,q=1){return !((i.unlockSpent??0)>s.totalSpent||s.runStatus!=='active')&&canRiskSpend(s,itemBulkPrice(i,s.owned[i.id]??0,q,s),netWorth(s))}export function buyItem(s:GameState,i:GameItem,q=1){const o=s.owned[i.id]??0,p=itemBulkPrice(i,o,q,s);if(q<=0||(i.unlockSpent??0)>s.totalSpent||!canRiskSpend(s,p,netWorth(s)))return s;return{...s,cash:s.cash-p,totalSpent:s.totalSpent+p,owned:{...s.owned,[i.id]:o+q},lowestCash:Math.min(s.lowestCash,s.cash-p),updatedAt:Date.now()}}export function canSellItem(s:GameState,i:GameItem){return s.runStatus==='active'&&(s.owned[i.id]??0)>0&&!(s.scenarioId==='custom'&&s.customScenario&&!s.customScenario.restrictions.sellingEnabled)}export function sellItem(s:GameState,i:GameItem,q=1){const o=s.owned[i.id]??0,n=Math.min(Math.max(0,q),o);if(!n||!canSellItem(s,i))return s;const refund=itemBulkPrice(i,o-n,n,s)*.7;return{...s,cash:s.cash+refund,totalSold:s.totalSold+refund,owned:{...s.owned,[i.id]:o-n},updatedAt:Date.now()}}export function upgradeHouse(s:GameState){const n=houseTiers.find(t=>t.level===s.houseLevel+1),c=n?n.cost*s.rules.economy.purchasePriceMultiplier:0;if(!n||!canRiskSpend(s,c,netWorth(s))||netWorth(s)<n.requiredNetWorth)return s;return{...s,cash:s.cash-c,totalSpent:s.totalSpent+c,houseLevel:n.level,lowestCash:Math.min(s.lowestCash,s.cash-c),updatedAt:Date.now()}}export function upgradeTown(s:GameState){if(s.houseLevel<5)return s;const n=townTiers.find(t=>t.level===s.townLevel+1),c=n?n.cost*s.rules.economy.purchasePriceMultiplier:0;if(!n||!canRiskSpend(s,c,netWorth(s))||netWorth(s)<n.requiredNetWorth)return s;return{...s,cash:s.cash-c,totalSpent:s.totalSpent+c,townLevel:n.level,lowestCash:Math.min(s.lowestCash,s.cash-c),updatedAt:Date.now()}}export function upgradeRegion(s:GameState){if(s.townLevel<5)return s;const n=regionTiers.find(t=>t.level===s.regionLevel+1),c=n?n.cost*s.rules.economy.purchasePriceMultiplier:0;if(!n||!canRiskSpend(s,c,netWorth(s))||netWorth(s)<n.requiredNetWorth)return s;return{...s,cash:s.cash-c,totalSpent:s.totalSpent+c,regionLevel:n.level,lowestCash:Math.min(s.lowestCash,s.cash-c),updatedAt:Date.now()}}export function upgradeCost(s:GameState,u:EmpireUpgrade){return u.baseCost*Math.pow(u.growthRate,s.upgrades?.[u.id]??0)*s.rules.economy.purchasePriceMultiplier}export function canBuyUpgrade(s:GameState,u:EmpireUpgrade){const l=s.upgrades?.[u.id]??0;if(l>=u.maxLevel||(u.requiredTownLevel??0)>s.townLevel||(u.requiredRegionLevel??0)>s.regionLevel||s.runStatus!=='active')return false;return canRiskSpend(s,upgradeCost(s,u),netWorth(s))}export function buyUpgrade(s:GameState,u:EmpireUpgrade){if(!canBuyUpgrade(s,u))return s;const l=s.upgrades?.[u.id]??0,c=upgradeCost(s,u);return{...s,cash:s.cash-c,totalSpent:s.totalSpent+c,upgrades:{...s.upgrades,[u.id]:l+1},lowestCash:Math.min(s.lowestCash,s.cash-c),updatedAt:Date.now()}}export function chooseCitySpecialization(s:GameState,x:CitySpecializationId){if(s.townLevel<4||s.citySpecialization||s.runStatus!=='active')return s;return{...s,citySpecialization:x,updatedAt:Date.now()}}export function totalOwned(s:GameState){return Object.values(s.owned).reduce((a,c)=>a+c,0)}export function unlockedAchievements(s:GameState):Achievement[]{const m={netWorth:netWorth(s),incomePerSecond:passiveCashPerSecond(s),totalOwned:totalOwned(s)};return achievements.filter(a=>achievementUnlockedByRule(s,a,m))}export function scenarioIsFreeMode(s:GameState){if(s.scenarioId==='custom'&&s.customScenario)return s.customScenario.winCondition.type==='free';return!!scenarios.find(e=>e.id===s.scenarioId)?.freeMode}export function scenarioDisplayName(s:GameState){if(s.scenarioId==='custom'&&s.customScenario)return s.customScenario.name;return scenarios.find(e=>e.id===s.scenarioId)?.name??'Scenario'}export function scenarioGoalLabel(s:GameState){if(s.scenarioId==='custom'&&s.customScenario)return customGoalLabel(s.customScenario);return scenarios.find(e=>e.id===s.scenarioId)?.goalLabel??'Build your empire'}export function scenarioProgress(s:GameState){if(s.scenarioId==='custom'&&s.customScenario){const{winCondition:w,startingCash:a}=s.customScenario,t=Math.max(.0001,w.target);if(w.type==='free')return 0;if(w.type==='net-worth')return Math.min(1,netWorth(s)/t);if(w.type==='total-spent')return Math.min(1,s.totalSpent/t);if(w.type==='wealth-multiplier')return a>0?Math.min(1,netWorth(s)/(a*t)):0;if(w.type==='income-per-second')return Math.min(1,Math.max(0,passiveCashPerSecond(s))/t);if(w.type==='town-level')return Math.min(1,s.townLevel/t);if(w.type==='region-level')return Math.min(1,s.regionLevel/t);if(w.type==='survive-minutes')return Math.min(1,s.activePlayMs/(t*60000));return 0}const x=scenarios.find(e=>e.id===s.scenarioId)??scenarios[0];if(x.freeMode)return 0;const m=s.rules.progression.scenarioGoalMultiplier;if(x.targetSpent)return Math.min(1,s.totalSpent/(x.targetSpent*m));if(x.targetNetWorth)return Math.min(1,netWorth(s)/(x.targetNetWorth*m));if(x.targetMultiplier&&x.startingCash>0)return Math.min(1,netWorth(s)/(x.startingCash*x.targetMultiplier*m));return 0}export function applyOfflineProgress(s:GameState,now=Date.now()){const x=normalizeGameState(s,now);if(!x.rules.world.offlineIncomeEnabled||x.riskMode||x.runStatus!=='active')return{...x,lastOfflineIncome:0};const o=calculateOfflineIncome(x,passiveCashPerSecond,now),i=o.income*x.rules.world.offlineIncomeMultiplier;if(i<=0)return{...x,lastOfflineIncome:0};return{...x,cash:x.cash+i,lifetimeIncome:x.lifetimeIncome+i,peakCash:Math.max(x.peakCash,x.cash+i),lastOfflineIncome:i,updatedAt:now}}export function advance(state:GameState,deltaMs:number){let s=normalizeGameState(state);if(s.runStatus!=='active')return s;const now=Date.now();s=s.rules.world.marketEventsEnabled?updateMarketEventState(s,now):(s.activeEventId?{...s,activeEventId:null,eventEndsAt:0}:s);s={...s,cityEconomy:advanceCityEconomy(s.cityEconomy,s.businesses??{},s.townLevel,deltaMs)};const prev=s.time.gameMinute,t=advanceTimeSimulation(s.time,deltaMs);s={...s,time:t.time};s=advanceLifeHousingCosts(s,prev,t.time.gameMinute);s=advanceCareerPay(s,prev,t.time.gameMinute);s=advanceEducation(s,prev,t.time.gameMinute);let activityIncome=0;if(t.completedActivityId){const a=activities.find(e=>e.id===t.completedActivityId);activityIncome=(a?.income??0)*s.rules.difficulty.activeIncomeMultiplier;if(a?.skillId&&a.skillXp)s={...s,life:gainLifeSkillXp(s.life,a.skillId,a.skillXp)}}const income=passiveCashPerSecond(s)*(deltaMs/1000)+activityIncome,lok=lokRuntime.accrue(s.lokTokens,s.lokProgressMs,deltaMs),cash=s.cash+income;let next={...s,cash,lifetimeIncome:s.lifetimeIncome+Math.max(0,income),activePlayMs:s.activePlayMs+deltaMs,lokTokens:lok.balance,lokProgressMs:lok.progressMs,lastOfflineIncome:0,lowestCash:Math.min(s.lowestCash,cash),updatedAt:now};let worth=netWorth(next);next={...next,peakCash:Math.max(next.peakCash,next.cash),peakNetWorth:Math.max(next.peakNetWorth,worth)};next=updateRiskState(next,worth,now);worth=netWorth(next);return syncAchievementUnlocks(next,achievements,{netWorth:worth,incomePerSecond:passiveCashPerSecond(next),totalOwned:totalOwned(next)},now)}